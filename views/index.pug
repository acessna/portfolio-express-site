extends layout.pug

block content 
  article.grid-container.portfolio-intro
      h1 Let's make something great for the web.

      p.lead.text-light
        | Below are several projects displaying how I have implemented different skills and technologies
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

    each project in data.projects
      .cell
        a(href='/projects/' + project.id)
          img.thumbnail(src= project.img_urls[0])
          h5 #{project.project_name}
      
